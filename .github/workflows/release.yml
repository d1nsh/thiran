name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build TypeScript
        run: npm run build

      - name: Create binaries
        run: npm run package:all

      - name: Prepare release binaries
        run: |
          mkdir -p dist/release
          cp dist/binaries/linux/thiran dist/release/thiran-linux
          cp dist/binaries/macos/thiran dist/release/thiran-macos
          cp dist/binaries/win/thiran.exe dist/release/thiran-win.exe
          chmod +x dist/release/thiran-linux dist/release/thiran-macos
          ls -lah dist/release/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/release/thiran-linux
            dist/release/thiran-macos
            dist/release/thiran-win.exe
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: binaries
          path: dist/release/
